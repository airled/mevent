<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Концерты</title>
    <link rel="stylesheet" href="styles.css">
  </head>
  <body>
    <main>
      <% @concerts.map do |concert| %>
        <div class="concert">
          <div class="pic"><img src=<%=concert.pic_url%>></div>
          <div class="name"><%=concert.name%></div>
          <div class="place"><%=concert.place%></div>
          <div class="date"><%=concert.date%></div>
        </div>
      <% end %>
    </main>
  </body>
</html>
